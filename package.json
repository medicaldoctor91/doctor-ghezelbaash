{
  "name": "doctor-ghezelbaash-astro",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/prepare-public.mjs && astro build && node scripts/validate-dist.mjs",
    "preview": "astro preview",
    "check:public": "node scripts/prepare-public.mjs",
    "validate:dist": "node scripts/validate-dist.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
