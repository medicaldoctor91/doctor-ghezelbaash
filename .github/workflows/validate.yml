name: Direct canonical graph source repair

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  repair-main:
    if: github.head_ref == 'direct-canonical-graph-fix-20260730'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1

      - uses: actions/setup-node@v4
        with:
          node-version: '24.18.0'
          cache: npm

      - name: Repair existing canonical graph sources
        shell: bash
        run: |
          set -euo pipefail
          python3 -m pip install --quiet rdflib
          python3 <<'PY'
          from pathlib import Path
          from rdflib import Graph
          from rdflib.compare import to_canonical_graph
          import base64, hashlib, json, re

          ROOT = Path('.')
          BASE = 'https://www.ghezelbaash.ir/'
          DATASET = BASE + '#historical-patient-origin-summary'
          LOGO = BASE + '#image-doctor-ghezelbaash-clinic-logo'
          ARTICLE_IMAGES = [
              {'@id': BASE + '#image-saeed-ghezelbash-portrait-square-1200'},
              {'@id': BASE + '#image-saeed-ghezelbash-portrait-4x3-1200'},
              {'@id': BASE + '#image-saeed-ghezelbash-portrait-16x9-1200'},
          ]
          ARTICLES = {
              BASE + '#article-omega-3-bipolar-i-2016': (
                  '2016-05-19T00:00:00Z',
                  'https://doi.org/10.4103/2008-7802.182734',
              ),
              BASE + '#article-mdd-attachment-dissociation-trauma-2021': (
                  '2021-09-06T00:00:00Z',
                  'https://doi.org/10.3390/healthcare9091169',
              ),
          }

          def type_set(node):
              value = node.get('@type')
              return set(value) if isinstance(value, list) else ({value} if isinstance(value, str) else set())

          def patch(document):
              graph = document.get('@graph')
              if not isinstance(graph, list):
                  raise RuntimeError('Missing @graph array')
              by_id = {
                  node.get('@id'): node
                  for node in graph
                  if isinstance(node, dict) and isinstance(node.get('@id'), str)
              }
              for node in graph:
                  if isinstance(node, dict) and 'ImageObject' in type_set(node) and isinstance(node.get('contentUrl'), str):
                      node['url'] = node['contentUrl']

              dataset = by_id.get(DATASET)
              if not isinstance(dataset, dict):
                  raise RuntimeError('Historical Dataset missing')
              dataset['spatialCoverage'] = [
                  {'@type': 'Place', 'name': 'Iran', 'alternateName': 'ایران', 'sameAs': 'https://www.wikidata.org/entity/Q794'},
                  {'@type': 'Place', 'name': 'Iraq', 'alternateName': 'عراق', 'sameAs': 'https://www.wikidata.org/entity/Q796'},
              ]

              logo = by_id.get(LOGO)
              if not isinstance(logo, dict) or not isinstance(logo.get('contentUrl'), str):
                  raise RuntimeError('Clinic logo ImageObject missing')
              logo.update({
                  'url': logo['contentUrl'],
                  'creditText': 'Dr. Saeed Ghezelbash Aesthetic Clinic',
                  'license': 'https://creativecommons.org/licenses/by/4.0/',
                  'acquireLicensePage': BASE + '#license',
                  'copyrightNotice': '© 2026 Dr. Saeed Ghezelbash',
              })

              for article_id, (date_value, doi_url) in ARTICLES.items():
                  article = by_id.get(article_id)
                  if not isinstance(article, dict):
                      raise RuntimeError(f'ScholarlyArticle missing: {article_id}')
                  article.update({
                      'datePublished': date_value,
                      'dateModified': date_value,
                      'url': doi_url,
                      'image': ARTICLE_IMAGES,
                  })
              return document

          head_path = ROOT / 'src/data/semantic/head-graph.min.jsonld'
          full_path = ROOT / 'public/graph.jsonld'
          ttl_path = ROOT / 'public/graph.ttl'

          head_doc = patch(json.loads(head_path.read_text(encoding='utf-8')))
          head_text = json.dumps(head_doc, ensure_ascii=False, separators=(',', ':')) + '\n'
          head_path.write_text(head_text, encoding='utf-8')

          full_doc = patch(json.loads(full_path.read_text(encoding='utf-8')))
          full_text = json.dumps(full_doc, ensure_ascii=False, indent=2) + '\n'
          full_path.write_text(full_text, encoding='utf-8')

          rdf = Graph().parse(data=full_text, format='json-ld')
          canonical = to_canonical_graph(rdf)
          nt = canonical.serialize(format='nt')
          ttl_path.write_text('\n'.join(sorted(line.strip() for line in nt.splitlines() if line.strip())) + '\n', encoding='utf-8')

          layout_path = ROOT / 'src/layouts/BaseLayout.astro'
          layout = layout_path.read_text(encoding='utf-8')
          layout = layout.replace("import { hardenRichResultsGraph } from '../lib/rich-results-graph.mjs';\n", '')
          layout = layout.replace('const structuredData = hardenRichResultsGraph(headGraph);\n', '')
          layout = layout.replace('const structuredData = headGraph;\n', '')
          layout = layout.replace('set:html={structuredData}', 'set:html={headGraph}')
          layout_path.write_text(layout, encoding='utf-8')

          package_path = ROOT / 'package.json'
          package = json.loads(package_path.read_text(encoding='utf-8'))
          package['scripts'] = {'build': 'astro build'}
          package_path.write_text(json.dumps(package, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')

          for obsolete in (
              ROOT / 'src/lib/rich-results-graph.mjs',
              ROOT / 'scripts/sync-rich-results-csp.mjs',
              ROOT / 'src/lib/page-projections.ts',
          ):
              obsolete.unlink(missing_ok=True)

          validator_path = ROOT / '.github/scripts/validate_source.py'
          validator = validator_path.read_text(encoding='utf-8')
          validator = validator.replace(
              '"src/pages/llms-full.txt.ts", "src/lib/page-projections.ts", "dist/llms-full.txt",',
              '"src/pages/llms-full.txt.ts", "dist/llms-full.txt",',
          )
          validator_path.write_text(validator, encoding='utf-8')

          digest = base64.b64encode(hashlib.sha256(head_text.encode('utf-8')).digest()).decode('ascii')
          headers_path = ROOT / 'public/_headers'
          headers = headers_path.read_text(encoding='utf-8')
          headers, count = re.subn(
              r"(script-src 'self' )'sha256-[A-Za-z0-9+/=]+';",
              rf"\1'sha256-{digest}';",
              headers,
              count=1,
          )
          if count != 1:
              raise RuntimeError('Failed to update CSP hash')
          headers_path.write_text(headers, encoding='utf-8')
          PY

          npm ci --no-audit --no-fund
          npm run build
          python3 .github/scripts/validate_source.py

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          if git diff --cached --quiet; then
            echo 'Canonical graph files already contain the direct corrections.'
            exit 0
          fi
          git commit -m 'Fix Rich Results directly in canonical graph sources'
          git push origin HEAD:main
