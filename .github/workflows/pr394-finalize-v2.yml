name: PR 394 superseded v2 finalizer archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Superseded by pr394-finalize-v4.yml."
