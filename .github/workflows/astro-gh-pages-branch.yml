name: Disabled gh-pages fallback

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "gh-pages fallback deployment is disabled. Canonical deployment remains GitHub Pages Actions from main."
