name: Deploy Astro to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    name: Build validated Astro dist
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        env:
          NPM_CONFIG_REGISTRY: https://registry.npmjs.org
        run: npm install --package-lock=false --no-audit --no-fund

      - name: Build and validate production dist
        run: npm run build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./dist

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy validated artifact
        id: deployment
        uses: actions/deploy-pages@v4

  verify:
    name: Verify custom domain deployment
    needs: deploy
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout deployed source
        uses: actions/checkout@v4

      - name: Verify live Homepage contract
        shell: bash
        run: |
          set -euo pipefail
          expected_h1='دکتر سعید قزلباش؛ پزشک زیبایی، پوست و مو در کرمانشاه'
          expected_person='id="mohammad-saeed-ghezelbash"'
          expected_testimonial='رضایت زیباجو از خدمات زیبایی دکتر سعید قزلباش'

          for attempt in $(seq 1 45); do
            url="https://www.ghezelbaash.ir/?deployment=${GITHUB_SHA}-${attempt}"
            html="$(curl -fsSL --retry 2 --retry-delay 2 -H 'Cache-Control: no-cache' -H 'Pragma: no-cache' "$url" || true)"

            if grep -Fq "$expected_h1" <<< "$html" \
              && grep -Fq "$expected_person" <<< "$html" \
              && grep -Fq "$expected_testimonial" <<< "$html"; then
              printf '%s\n' 'Verified final Homepage contract on the custom domain.'
              exit 0
            fi

            sleep 20
          done

          printf '%s\n' 'Custom domain did not expose the deployed Homepage contract within the verification window.' >&2
          exit 1

      - name: Record verified deployment commit
        shell: bash
        run: git push --force origin "${GITHUB_SHA}:refs/heads/deployment-live"
