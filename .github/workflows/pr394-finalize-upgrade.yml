name: PR 394 final conformance transaction

on:
  push:
    branches:
      - candidate/final-repository-plan-2026

permissions:
  contents: write

concurrency:
  group: pr394-final-conformance
  cancel-in-progress: false

jobs:
  finalize:
    runs-on: ubuntu-24.04
    timeout-minutes: 70
    steps:
      - name: Checkout exact PR branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: candidate/final-repository-plan-2026
          fetch-depth: 0

      - name: Set up pinned Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Set up pinned Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.14.6'

      - name: Provision media validation tools
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Materialize exact final candidate
        shell: bash
        run: |
          set -euo pipefail
          INITIAL_HEAD="$(git rev-parse HEAD)"
          test "$INITIAL_HEAD" = "$GITHUB_SHA"

          python - <<'PY'
          from pathlib import Path
          raw = Path('.github/workflows/pr394-apply-upgrade.yml').read_text()
          start = "          cat > scripts/validate-rdf-isomorphism.mjs <<'EOF'\n"
          end = "          EOF\n"
          assert start in raw
          tail = raw.split(start, 1)[1]
          body, _ = tail.split(end, 1)
          lines = []
          for line in body.splitlines():
              assert line.startswith('          ') or not line
              lines.append(line[10:] if line.startswith('          ') else line)
          Path('/tmp/validate-rdf-isomorphism.mjs').write_text('\n'.join(lines) + '\n')
          PY

          cat .pr394-upgrade.gz.b64.* | base64 --decode | gzip -dc > /tmp/pr394-upgrade.patch
          git apply --check /tmp/pr394-upgrade.patch
          git apply /tmp/pr394-upgrade.patch

          python - <<'PY'
          from pathlib import Path
          import re

          p = Path('scripts/validate-cloudflare-pages-limits.mjs')
          s = p.read_text()
          old = '/' + 'media/images/physician/' + 'example.webp'
          assert old in s
          p.write_text(s.replace(old, '/assets/image-example.webp', 1))

          p = Path('src/components/GuideNavigator.astro')
          s = p.read_text()
          old = 'dialogOpener = opener instanceof HTMLElement ? opener : null;'
          new = 'dialogOpener = opener && typeof opener.focus === "function" ? opener : null;'
          assert old in s
          s = s.replace(old, new, 1)
          old = 'if (target?.isConnected)\n      requestAnimationFrame(() => target.focus({ preventScroll: true }));'
          new = 'if (target && target.isConnected !== false && typeof target.focus === "function")\n      requestAnimationFrame(() => target.focus({ preventScroll: true }));'
          assert old in s
          p.write_text(s.replace(old, new, 1))

          p = Path('scripts/test-guide-navigator.mjs')
          s = p.read_text()
          old = 'dialog.close = () => { dialog.open = false; };'
          new = 'dialog.close = () => { dialog.open = false; emit(dialog, "close"); };'
          assert old in s
          p.write_text(s.replace(old, new, 1))

          p = Path('scripts/lib/strict-json.mjs')
          s = p.read_text()
          old_signature = 'export function parseJsonNoDuplicateKeys(text, label = "JSON") {'
          new_signature = 'export function parseJsonNoDuplicateKeys(text, label = "JSON", { materialize = true } = {}) {'
          assert old_signature in s
          s = s.replace(old_signature, new_signature, 1)
          old_primitive = (
              '  const primitive = () => {\n'
              '    const rest = text.slice(i);\n'
              '    const match = rest.match(/^(?:true|false|null|-?(?:0|[1-9]\\d*)(?:\\.\\d+)?(?:[eE][+-]?\\d+)?)/u);\n'
              '    if (!match) fail("invalid value");\n'
              '    i += match[0].length;\n'
              '  };'
          )
          new_primitive = (
              '  const primitivePattern = /(?:true|false|null|-?(?:0|[1-9]\\d*)(?:\\.\\d+)?(?:[eE][+-]?\\d+)?)/uy;\n'
              '  const primitive = () => {\n'
              '    primitivePattern.lastIndex = i;\n'
              '    const match = primitivePattern.exec(text);\n'
              '    if (!match) fail("invalid value");\n'
              '    i += match[0].length;\n'
              '  };'
          )
          assert old_primitive in s
          s = s.replace(old_primitive, new_primitive, 1)
          old_return = '  return JSON.parse(text);'
          new_return = '  return materialize ? JSON.parse(text) : undefined;'
          assert old_return in s
          s = s.replace(old_return, new_return, 1)
          p.write_text(s)

          p = Path('scripts/validate-artifact-semantics.mjs')
          s = p.read_text()
          assert 'query-matrix.jsonl' in s and 'query-matrix.json-seq' in s
          old_json_loop = (
              'const parsedJson = new Map();\n'
              'for (const file of strictJsonFiles) parsedJson.set(file, parseJsonNoDuplicateKeys(await readFile(path.join(root, file), "utf8"), `dist/${file}`));'
          )
          new_json_loop = (
              'const parsedJson = new Map();\n'
              'const retainedJsonFiles = new Set(["site.webmanifest", "linkset.json", "entity-facts.csv-metadata.json", "datapackage.json", "croissant.json"]);\n'
              'for (const file of strictJsonFiles) {\n'
              '  const keep = retainedJsonFiles.has(file);\n'
              '  const parsed = parseJsonNoDuplicateKeys(await readFile(path.join(root, file), "utf8"), `dist/${file}`, { materialize: keep });\n'
              '  if (keep) parsedJson.set(file, parsed);\n'
              '}'
          )
          assert old_json_loop in s
          s = s.replace(old_json_loop, new_json_loop, 1)

          old_tel = 'for (const row of properties.filter((x) => x.name === "TEL")) assert.match(row.value, /^\\+?[0-9][0-9(). -]*$/u, `${file} telephone lexical form invalid`);'
          new_tel = 'for (const row of properties.filter((x) => x.name === "TEL")) assert.match(row.value, /^tel:\\+?[0-9][0-9(). -]*$/u, `${file} TEL must be an RFC 6350 URI value`);'
          assert old_tel in s
          s = s.replace(old_tel, new_tel, 1)

          marker = 'const ndjson = await readFile('
          idx = s.find(marker)
          assert idx >= 0
          helper = (
              'const machineResourceRegistry = JSON.parse(await readFile("src/data/machine-resources.json", "utf8"));\n'
              'const resolveRegisteredArtifact = (resourcePath) => {\n'
              '  const resource = machineResourceRegistry.resources.find((entry) => entry.path === resourcePath);\n'
              '  if (!resource) throw new Error(`Machine resource is not registered: ${resourcePath}`);\n'
              '  return resource.targets?.includes("website") ? path.join(root, resource.path) : resource.source;\n'
              '};\n\n'
          )
          s = s[:idx] + helper + s[idx:]
          lines = s.splitlines()
          replaced = set()
          for i, line in enumerate(lines):
              for name in ('query-matrix.jsonl', 'query-matrix.json-seq'):
                  if name not in line or 'readFile(' not in line:
                      continue
                  pattern = re.compile(rf'readFile\([^;\n]*{re.escape(name)}[^;\n]*\)')
                  updated, count = pattern.subn(f'readFile(resolveRegisteredArtifact("{name}"), "utf8")', line, count=1)
                  if count == 1:
                      lines[i] = updated
                      replaced.add(name)
          assert replaced == {'query-matrix.jsonl', 'query-matrix.json-seq'}, (replaced, [line for line in lines if 'query-matrix' in line])
          s = '\n'.join(lines) + ('\n' if s.endswith('\n') else '')
          for name in ('query-matrix.jsonl', 'query-matrix.json-seq'):
              assert f'resolveRegisteredArtifact("{name}")' in s

          separator_lines = [line for line in s.splitlines() if 'assert.equal' in line and '0x1e' in line.lower()]
          assert len(separator_lines) == 1, separator_lines
          fixed_line, rs_count = re.subn(
              r'(?<![A-Za-z0-9_])0x1[eE](?![A-Za-z0-9_])',
              lambda _match: r'"\x1e"',
              separator_lines[0], count=1,
          )
          fixed_line, lf_count = re.subn(
              r'(?<![A-Za-z0-9_])0x0[aA](?![A-Za-z0-9_])',
              lambda _match: r'"\n"',
              fixed_line, count=1,
          )
          assert rs_count == 1 and lf_count == 1 and fixed_line != separator_lines[0], (rs_count, lf_count, separator_lines[0])
          s = s.replace(separator_lines[0], fixed_line, 1)
          p.write_text(s)

          Path('scripts/validate-rdf-isomorphism.mjs').write_text(Path('/tmp/validate-rdf-isomorphism.mjs').read_text())
          PY

          mv scripts/audit-performance.mjs scripts/measure-performance.mjs
          sed -i 's|scripts/audit-performance.mjs|scripts/measure-performance.mjs|g' package.json .github/workflows/ci.yml
          sed -i 's/await chrome.kill();/chrome.kill();/' scripts/measure-performance.mjs
          sed -i 's/^on:$/on:\n  workflow_dispatch:/' .github/workflows/ci.yml

          npm install --package-lock-only --ignore-scripts --no-audit --no-fund

          rm -f .pr394-upgrade.gz.b64.*
          rm -f .github/workflows/pr394-apply-upgrade.yml
          rm -f .github/workflows/pr394-finalize-upgrade.yml
          rm -f .github/workflows/pr394-artifact-source-diagnostic.yml

          npm ci
          npm run security:dependencies
          npx playwright install --with-deps chromium
          npm run render:calibration:update
          npm run test:render-navigation -- .generated/calibration-candidate

          node --input-type=module <<'EOF'
          import assert from 'node:assert/strict';
          import { parseJsonNoDuplicateKeys } from './scripts/lib/strict-json.mjs';
          assert.deepEqual(parseJsonNoDuplicateKeys('{"a":1,"b":[true,null,-2.5e3]}'), { a: 1, b: [true, null, -2500] });
          assert.equal(parseJsonNoDuplicateKeys('{"a":1,"b":2}', 'validation-only', { materialize: false }), undefined);
          assert.throws(() => parseJsonNoDuplicateKeys('{"a":1,"a":2}'), /duplicate object key/u);
          EOF

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'feat: harden 2026 dist conformance and browser evidence'

          FINAL_HEAD="$(git rev-parse HEAD)"
          SOURCE_DATE_EPOCH="$(git show -s --format=%ct "$FINAL_HEAD")"
          {
            echo "FINAL_HEAD=$FINAL_HEAD"
            echo "SOURCE_COMMIT=$FINAL_HEAD"
            echo "CF_PAGES_COMMIT_SHA=$FINAL_HEAD"
            echo "SOURCE_DATE_EPOCH=$SOURCE_DATE_EPOCH"
          } >> "$GITHUB_ENV"
          printf '%s\n' "$FINAL_HEAD" > /tmp/pr394-final-head.txt
          printf 'Prepared final candidate %s\n' "$FINAL_HEAD"

      - name: Release final candidate
        shell: bash
        run: |
          set +e
          npm run release 2>&1 | tee /tmp/pr394-release.log
          rc=${PIPESTATUS[0]}
          printf '%s\n' "$rc" > /tmp/pr394-release-exit.txt
          exit "$rc"

      - name: Capture transaction evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: pr394-final-transaction-${{ github.sha }}
          path: |
            /tmp/pr394-final-head.txt
            /tmp/pr394-release.log
            /tmp/pr394-release-exit.txt
            .generated/validation/
            .release/runtime/
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 7

      - name: Push only fully validated candidate
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$FINAL_HEAD"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"
          REMOTE_HEAD="$(git ls-remote origin refs/heads/candidate/final-repository-plan-2026 | cut -f1)"
          test "$REMOTE_HEAD" = "$GITHUB_SHA"
          git push origin HEAD:candidate/final-repository-plan-2026
          printf 'FINAL_HEAD=%s\n' "$FINAL_HEAD"
