name: PR 394 superseded finalizer archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Superseded by the exact-head final transaction."
