name: Temporary clinic Wikidata constraint audit

on:
  workflow_run:
    workflows: ['Read-only source and release validation']
    types: [completed]

permissions:
  contents: write

jobs:
  audit:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main' &&
      !contains(github.event.workflow_run.head_commit.message, 'Record temporary clinic constraint audit')
    runs-on: macos-15
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: main
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: '3.14.6'
      - run: python -m pip install --disable-pip-version-check --no-input requests==2.32.5
      - name: Audit live statements and constraints
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p wikimedia
          python scripts/_tmp_audit_q140288589_constraints.py > wikimedia/_tmp-q140288589-constraint-audit.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add wikimedia/_tmp-q140288589-constraint-audit.json
          git commit -m 'Record temporary clinic constraint audit'
          git push origin HEAD:main
