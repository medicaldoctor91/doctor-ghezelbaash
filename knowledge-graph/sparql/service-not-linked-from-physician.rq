PREFIX schema: <https://schema.org/>

SELECT ?service WHERE {
  ?service a schema:Service .
  FILTER NOT EXISTS { <https://www.ghezelbaash.ir/#physician> schema:makesOffer ?service . }
  FILTER NOT EXISTS { <https://www.ghezelbaash.ir/#physician> schema:availableService ?service . }
}
