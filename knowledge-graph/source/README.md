# Graph source

The current graph is generated for audit from the repository source and tracked against the baseline manifest.

This folder is the graph-first source control layer. The full graph can be materialized by the Graph Quality workflow without deploying the website.
