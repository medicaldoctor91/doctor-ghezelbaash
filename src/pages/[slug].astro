---
import BaseLayout from '../layouts/BaseLayout.astro';
import ServiceLayout from '../layouts/ServiceLayout.astro';
import { site } from '../data/site.mjs';
import { services } from '../data/services.mjs';

export function getStaticPaths() {
  const servicePaths = services.map((service) => ({ params: { slug: service.slug }, props: { service, kind: 'service' } }));
  return [{ params: { slug: 'about' }, props: { kind: 'about' } }, ...servicePaths];
}

const { service, kind } = Astro.props;
const isAbout = kind === 'about';
---
{isAbout &&
  <BaseLayout title={`درباره ${site.personFa}`} description="معرفی رسمی دکتر سعید قزلباش." canonicalPath="/about/">
    <section class="box">
      <p class="small">درباره</p>
      <h1>درباره {site.personFa}</h1>
      <p>{site.personFa} پزشک زیبایی در کرمانشاه است.</p>
      <p><a class="cta" href={site.instagram}>اینستاگرام</a> <a class="cta-secondary" href="/contact/">تماس</a></p>
    </section>
  </BaseLayout>
}
{!isAbout && <ServiceLayout service={service} />}
