# Schema data

Active structured data is generated from `ghezelbaash-page-graphs.json`.

Legacy global schema files were intentionally removed from the active project to avoid noise, duplicated nodes, dead URLs, and accidental injection of old-domain data. Raw entity information from old graphs was extracted and merged directly into the active page graph, service catalog, and datasets.
