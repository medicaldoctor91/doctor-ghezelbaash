---
import type { Graph } from 'schema-dts';

type Props = {
  data: Graph;
};

const { data } = Astro.props;
const serializedData = JSON.stringify(data).replaceAll('<', '\\u003c');
---

<script is:inline type="application/ld+json" set:html={serializedData}></script>
