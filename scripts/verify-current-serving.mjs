import {createHash} from 'node:crypto';
import {readFile} from 'node:fs/promises';
const release=JSON.parse(await readFile('src/data/release.json','utf8')),volatile=JSON.parse(await readFile('src/data/volatile-facts.json','utf8')),sha=b=>createHash('sha256').update(b).digest('hex');
const currentFiles=['index.html','graph.jsonld','graph.ttl','entity-facts.csv','answers.txt','knowledge.xml','llms.txt','index.md','llms-full.txt','datapackage.json','linkset.json','void.ttl','dcat.ttl','croissant.json','provenance.jsonld','evidence-snapshot.json','shapes.ttl','artifact-manifest.json','query-matrix.jsonl','current-release-matrix.json','live-observations.jsonld','live-serving-attestation.json'];
const fetchOne=async(url,noCache=false)=>{const headers={'User-Agent':'ghezelbaash-current-serving-verifier/1.0'};if(noCache)headers['Cache-Control']='no-cache';const r=await fetch(url,{headers,signal:AbortSignal.timeout(60000)});if(!r.ok)throw new Error(`HTTP ${r.status} ${url}`);return {r,b:Buffer.from(await r.arrayBuffer())}};
const results=[];for(const file of currentFiles){const local=await readFile(`dist/${file}`),wanted=sha(local),url=`${release.canonicalUrl}${file==='index.html'?'':file}`,ordinary=await fetchOne(url),bypass=await fetchOne(`${url}${url.includes('?')?'&':'?'}__serving_verify=${Date.now()}`,true),oh=sha(ordinary.b),bh=sha(bypass.b);if(oh!==wanted||bh!==wanted)throw new Error(`Current serving byte drift ${file}: ordinary=${oh} bypass=${bh} wanted=${wanted}`);if(['artifact-manifest.json','live-observations.jsonld','current-release-matrix.json','live-serving-attestation.json'].includes(file)){const cc=ordinary.r.headers.get('cache-control')||'';if(!/max-age=0|must-revalidate/i.test(cc))throw new Error(`Mutable/current machine resource cache policy too stale ${file}: ${cc}`)}results.push({file,sha256:wanted,ordinary:ordinary.r.status,bypass:bypass.r.status,cfCacheStatus:ordinary.r.headers.get('cf-cache-status'),age:ordinary.r.headers.get('age')})}
const live=JSON.parse(await readFile('dist/live-observations.jsonld','utf8')),props=Array.isArray(live.item?.additionalProperty)?live.item.additionalProperty:[],rating=Number(props.find(x=>x.propertyID==='Google Places rating')?.value),reviewCount=Number(props.find(x=>x.propertyID==='Google Places userRatingCount')?.value);if(live.about?.['@id']!==release.clinic.id||live.dateModified!==volatile.valueObservedAt||rating!==Number(volatile.rating)||reviewCount!==Number(volatile.reviewCount))throw new Error('Current website live observation drift');
const hfCsv=(await fetchOne(`https://huggingface.co/datasets/doctor-ghezelbaash/dr-saeid-ghezelbaash-entity-data/resolve/main/live_observations.csv?download=true&_=${Date.now()}`,true)).b.toString();for(const token of [release.clinic.id,release.clinic.placeId,String(rating),String(reviewCount),volatile.valueObservedAt,release.release])if(!hfCsv.includes(token))throw new Error(`HF main live observation drift: missing ${token}`);
const hfReadme=(await fetchOne(`https://huggingface.co/datasets/doctor-ghezelbaash/dr-saeid-ghezelbaash-entity-data/resolve/main/README.md?download=true&_=${Date.now()}`,true)).b.toString();for(const token of [release.release,release.dataset.zenodo.versionDoi,'text-retrieval','text-generation','query_matrix','live_observations',release.primaryEntity.wikidata])if(!hfReadme.includes(token))throw new Error(`HF current card drift: missing ${token}`);
console.log(JSON.stringify({stage:'CURRENT_SERVING_TRUTH',release:release.release,currentReputation:{rating,reviewCount,valueObservedAt:volatile.valueObservedAt},websiteExact:true,hfLiveObservationExact:true,files:results.length,integrity:'PASS',results},null,2));
