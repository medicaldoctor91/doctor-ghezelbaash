import path from 'node:path';
import {createHash} from 'node:crypto';
import {readFile,writeFile,readdir} from 'node:fs/promises';
const root=process.cwd(),dist=path.resolve(root,process.argv[2]||'dist'),data=path.join(root,'src/data');
const release=JSON.parse(await readFile(path.join(data,'release.json'),'utf8'));
const volatile=JSON.parse(await readFile(path.join(data,'volatile-facts.json'),'utf8'));
const shaHex=b=>createHash('sha256').update(b).digest('hex'),shaB64=b=>createHash('sha256').update(b).digest('base64');
async function walk(d,p=''){let out=[];for(const e of (await readdir(d,{withFileTypes:true})).sort((a,b)=>a.name.localeCompare(b.name))){const a=path.join(d,e.name),r=p?`${p}/${e.name}`:e.name;if(e.isDirectory())out.push(...await walk(a,r));else if(e.isFile())out.push({abs:a,rel:r});}return out}
const liveRevision=process.env.CF_PAGES_COMMIT_SHA||process.env.GITHUB_SHA||process.env.SOURCE_COMMIT||'local-unbound';
const generatedAt=process.env.SOURCE_DATE_EPOCH?new Date(Number(process.env.SOURCE_DATE_EPOCH)*1000).toISOString():`${release.dateModified}T00:00:00.000Z`;
const createdAt=(release.dataset.zenodo.releaseHistory||[]).map(x=>x.publicationDate).sort()[0]||release.dateModified;
const datasetName=release.dataset.name;
// Normalize descriptors produced by the legacy finalizer while retaining its complete integrity machinery.
const dataPackagePath=path.join(dist,'datapackage.json');const dp=JSON.parse(await readFile(dataPackagePath,'utf8'));dp.name='dr-saeed-ghezelbash-public-knowledge-graph';dp.title=`${datasetName} — Data Package`;dp.description='Physician-owned first-party knowledge graph, direct-answer, evidence, provenance and retrieval resources for Dr. Saeed Ghezelbash and the supporting clinic.';dp.version=release.release;dp.created=createdAt;dp.lastUpdated=release.dateModified;await writeFile(dataPackagePath,JSON.stringify(dp,null,2)+'\n');
const croissantPath=path.join(dist,'croissant.json');const cr=JSON.parse(await readFile(croissantPath,'utf8'));cr.name=datasetName;cr.description='Physician-owned first-party knowledge graph Dataset for Dr. Saeed Ghezelbash, the supporting clinic, services, answers, provenance and machine retrieval.';cr.version=release.release;cr.dateCreated=createdAt;cr.datePublished=release.dateModified;cr.dateModified=release.dateModified;cr.isLiveDataset=false;await writeFile(croissantPath,JSON.stringify(cr,null,2)+'\n');
const dcatPath=path.join(dist,'dcat.ttl');let dcat=await readFile(dcatPath,'utf8');dcat=dcat.replace('"Doctor Ghezelbash Structured Data Catalog"@en',`"${datasetName} — Data Catalog"@en`);await writeFile(dcatPath,dcat);
const linksetPath=path.join(dist,'linkset.json');const linkset=JSON.parse(await readFile(linksetPath,'utf8'));const rootSet=linkset.linkset?.[0];if(!rootSet)throw new Error('linkset root missing');rootSet.describedby=rootSet.describedby||[];for(const x of [{href:`${release.canonicalUrl}query-matrix.jsonl`,type:'application/jsonl'},{href:`${release.canonicalUrl}live-observations.jsonld`,type:'application/ld+json'},{href:`${release.canonicalUrl}current-release-matrix.json`,type:'application/json'}])if(!rootSet.describedby.some(y=>y.href===x.href))rootSet.describedby.push(x);await writeFile(linksetPath,JSON.stringify(linkset,null,2)+'\n');
// Current serving manifest describes current bytes; Zenodo/HF retain the release-time copy frozen at publication.
const manifestPath=path.join(dist,'artifact-manifest.json');const manifest=JSON.parse(await readFile(manifestPath,'utf8'));manifest.architecture='Astro pure-static single-page SSG with physician-first identity graph, immutable release snapshot semantics, separate mutable live observations, Query Matrix 2.0, provenance, current-serving integrity and Cloudflare Git deployment.';manifest.generatedAt=generatedAt;manifest.release=release.release;manifest.baseRelease=release.release;manifest.liveRevision=liveRevision;manifest.dataset={id:release.dataset.id,name:datasetName,wikidata:release.dataset.wikidata,conceptDoi:release.dataset.zenodo.conceptDoi,versionDoi:release.dataset.zenodo.versionDoi,zenodoRecordId:String(release.dataset.zenodo.recordId)};manifest.review={date:release.medicalReviewedAt,reviewedBy:release.reviewedBy};manifest.liveObservation={url:`${release.canonicalUrl}live-observations.jsonld`,entity:release.clinic.id,placeId:release.clinic.placeId,rating:Number(volatile.rating),reviewCount:Number(volatile.reviewCount),valueObservedAt:volatile.valueObservedAt};manifest.integrity={...(manifest.integrity||{}),algorithm:'SHA-256',excluded:['artifact-manifest.json','_headers','live-serving-attestation.json'],note:'Current manifest excludes itself, deployment headers and terminal live-serving attestation, preventing circular integrity dependencies.'};
const files={};for(const f of await walk(dist)){if(['artifact-manifest.json','_headers','live-serving-attestation.json'].includes(f.rel))continue;const b=await readFile(f.abs);files[f.rel]={bytes:b.length,sha256:shaHex(b)}}manifest.files=files;const html=await readFile(path.join(dist,'index.html'));manifest.invariants={...(manifest.invariants||{}),htmlBytes:html.length,rootHtmlSha256:shaHex(html),releaseDerived:true};await writeFile(manifestPath,JSON.stringify(manifest,null,2)+'\n');
let headers=await readFile(path.join(dist,'_headers'),'utf8');
const routeBlock=(route,media,cache,digest=true)=>`\n/${route}\n  Content-Type: ${media}\n  X-Robots-Tag: index, follow, max-snippet:-1\n  X-Robots-Tag: googlebot: noindex, follow\n  Cache-Control: ${cache}\n  Cloudflare-CDN-Cache-Control: ${cache}\n  Link: <${release.canonicalUrl}${route}>; rel="canonical", <${release.dataset.id}>; rel="describedby", <${release.clinic.id}>; rel="about"\n${digest?`  Repr-Digest: sha-256=:__DIGEST__:\n`:''}  Access-Control-Allow-Origin: *\n  Access-Control-Expose-Headers: Link, Repr-Digest, Content-Signal\n  Cross-Origin-Resource-Policy: cross-origin\n`;
const ensureBlock=(route,media,cache,digest=true)=>{if(!new RegExp(`(?:^|\\n)/${route.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')}\\n`).test(headers))headers+=routeBlock(route,media,cache,digest)};
ensureBlock('query-matrix.jsonl','application/jsonl; charset=utf-8','public, max-age=3600, must-revalidate');ensureBlock('live-observations.jsonld','application/ld+json; charset=utf-8','public, max-age=0, must-revalidate');ensureBlock('current-release-matrix.json','application/json; charset=utf-8','public, max-age=0, must-revalidate');ensureBlock('live-serving-attestation.json','application/json; charset=utf-8','public, max-age=0, must-revalidate',false);
const replaceDigest=(route,b64)=>{const esc=route.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');const re=new RegExp(`((?:^|\\n)/${esc}\\n[\\s\\S]*?Repr-Digest: sha-256=:)([^:]*)(:\\n)(?=\\s{2}|\\/|$)`);if(re.test(headers))headers=headers.replace(re,`$1${b64}$3`);else if(headers.includes(`/${route}\n`))headers=headers.replace(`/${route}\n`,`/${route}\n  Repr-Digest: sha-256=:${b64}:\n`)};
for(const route of ['artifact-manifest.json','datapackage.json','croissant.json','dcat.ttl','linkset.json','query-matrix.jsonl','live-observations.jsonld','current-release-matrix.json'])replaceDigest(route,shaB64(await readFile(path.join(dist,route))));
// Current machine resources should not remain stale for a week at the CDN after a release/live change.
headers=headers.replaceAll('Cloudflare-CDN-Cache-Control: public, max-age=604800, stale-while-revalidate=2592000','Cloudflare-CDN-Cache-Control: public, max-age=3600, must-revalidate, stale-if-error=86400');
if(!headers.includes(`${release.canonicalUrl}live-observations.jsonld`))headers=headers.replace(/(Link: <https:\/\/www\.ghezelbaash\.ir\/entity-facts\.csv>[^\n]*\n)/,`$1  Link: <${release.canonicalUrl}query-matrix.jsonl>; rel="describedby"; type="application/jsonl", <${release.canonicalUrl}live-observations.jsonld>; rel="describedby"; type="application/ld+json", <${release.canonicalUrl}current-release-matrix.json>; rel="describedby"; type="application/json"\n`);
await writeFile(path.join(dist,'_headers'),headers);
const headersBytes=await readFile(path.join(dist,'_headers')),manifestBytes=await readFile(manifestPath),liveBytes=await readFile(path.join(dist,'live-observations.jsonld'));
const visibleContract=JSON.parse(await readFile(path.join(data,'visible-contract.json'),'utf8'));
const attestation={schemaVersion:'1.0',baseRelease:release.release,liveRevision,generatedAt,sourceCommit:liveRevision,artifactManifestSha256:shaHex(manifestBytes),indexHtmlSha256:shaHex(html),liveObservationSha256:shaHex(liveBytes),headersSha256:shaHex(headersBytes),visibleContractPolicy:visibleContract.policy,mutableVisibleSelector:visibleContract.mutableAfterReleaseSelector,conceptDoi:release.dataset.zenodo.conceptDoi,versionDoi:release.dataset.zenodo.versionDoi,integrity:'CURRENT_SERVING'};
await writeFile(path.join(dist,'live-serving-attestation.json'),JSON.stringify(attestation,null,2)+'\n');
console.log(JSON.stringify({finalizedV122:true,release:release.release,liveRevision,manifestSha256:attestation.artifactManifestSha256,headersSha256:attestation.headersSha256,liveObservationSha256:attestation.liveObservationSha256},null,2));
