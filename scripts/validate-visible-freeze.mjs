import {spawnSync} from 'node:child_process';
import {readFile} from 'node:fs/promises';
const html=process.argv[2]||'dist/index.html',contract=JSON.parse(await readFile('src/data/visible-contract.json','utf8'));if(!/^[0-9a-f]{64}$/.test(contract.finalVisibleDomSha256||''))throw new Error('Visible contract is not final-locked');const r=spawnSync(process.execPath,['scripts/compute-visible-contract.mjs',html],{encoding:'utf8'});if(r.status!==0)throw new Error(r.stderr||r.stdout);const result=JSON.parse(r.stdout);if(result.sha256!==contract.finalVisibleDomSha256)throw new Error(`Visible DOM freeze violation: current=${result.sha256} expected=${contract.finalVisibleDomSha256}`);console.log(JSON.stringify({visibleFreeze:true,sha256:result.sha256,records:result.records,mutableSelector:contract.mutableAfterReleaseSelector},null,2));