import {createHash} from 'node:crypto';
import {mkdir,readFile,readdir,writeFile} from 'node:fs/promises';
import path from 'node:path';
const release=JSON.parse(await readFile('src/data/release.json','utf8')),sha=async file=>createHash('sha256').update(await readFile(file)).digest('hex'),walk=async dir=>{let total=0;for(const e of await readdir(dir,{withFileTypes:true}))total+=e.isDirectory()?await walk(path.join(dir,e.name)):e.isFile()?1:0;return total};
const sourceCommit=(process.env.SOURCE_COMMIT||process.env.GITHUB_SHA||'').trim();if(!/^[0-9a-f]{40}$/.test(sourceCommit))throw new Error('SOURCE_COMMIT must be the exact 40-character frozen Candidate SHA');
const z=release.dataset.zenodo,attestation={schema:'https://www.ghezelbaash.ir/release-attestation/v3',release:release.release,releasePublishedAt:release.dateModified,medicalReviewedAt:release.medicalReviewedAt,canonicalDatasetIri:release.dataset.id,primaryEntity:release.primaryEntity.wikidata,clinicEntity:release.dataset.supportingClinicWikidata,datasetEntity:release.dataset.wikidata,sourceRepository:release.dataset.github.repository,sourceCommit,zenodoConceptDoi:z.conceptDoi,zenodoVersionDoi:z.versionDoi,zenodoRecordId:String(z.recordId),releaseHistory:z.releaseHistory,artifactManifestSha256:await sha('dist/artifact-manifest.json'),graphJsonldSha256:await sha('dist/graph.jsonld'),graphTurtleSha256:await sha('dist/graph.ttl'),indexHtmlSha256:await sha('dist/index.html'),queryMatrixSha256:await sha('dist/query-matrix.jsonl'),currentReleaseMatrixSha256:await sha('dist/current-release-matrix.json'),distFileCount:await walk('dist'),validation:'PASS',distributionRoles:{website:'canonical-first-party-dataset',github:'version-controlled-source',zenodo:'immutable-doi-preservation',huggingFace:'ai-retrieval-distribution',huggingFaceCore:'release-faithful-core',huggingFaceQueryMatrix:'maximum-retrieval-positioning',liveObservations:'mutable-clinic-reputation-overlay'}};
await mkdir('.release/runtime',{recursive:true});await writeFile('.release/runtime/release-attestation.json',JSON.stringify(attestation,null,2)+'\n');console.log(JSON.stringify(attestation,null,2));
