import path from 'node:path';
import {createHash} from 'node:crypto';
import {readFile} from 'node:fs/promises';
const root=process.cwd(),base=process.env.VERIFY_BASE_URL||'https://www.ghezelbaash.ir/',stable=JSON.parse(await readFile(path.join(root,'src/data/stable-media-aliases.json'),'utf8'));
const core=['index.html','graph.jsonld','graph.ttl','entity-facts.csv','answers.txt','knowledge.xml','llms.txt','index.md','llms-full.txt','datapackage.json','linkset.json','void.ttl','dcat.ttl','croissant.json','provenance.jsonld','evidence-snapshot.json','shapes.ttl','artifact-manifest.json','query-matrix.jsonl','current-release-matrix.json','live-observations.jsonld','live-serving-attestation.json','ns/entity-metadata/2026/index.html','ns/media-semantics/2026/index.html'];
const files=[...new Set([...core,...stable.aliases.map(row=>row.path)])].sort(),sha=bytes=>createHash('sha256').update(bytes).digest('hex'),routeFor=rel=>rel==='index.html'?'':rel.endsWith('/index.html')?rel.slice(0,-10):rel;
const expected=new Map();for(const rel of files)expected.set(rel,await readFile(path.join(root,'dist',rel)));
const fetchBytes=async url=>{const response=await fetch(url,{headers:{'user-agent':'ghezelbaash-pages-byte-verifier/2.0',accept:'*/*'},redirect:'follow',signal:AbortSignal.timeout(45000)});return{response,bytes:Buffer.from(await response.arrayBuffer())}};
const verifyOne=async rel=>{const wanted=expected.get(rel),wantedSha=sha(wanted),url=new URL(routeFor(rel),base);for(let attempt=1;attempt<=24;attempt++){const ordinary=await fetchBytes(url),bypass=new URL(url);bypass.searchParams.set('__pages_byte_verify',`${Date.now()}-${attempt}`);const fresh=await fetchBytes(bypass),ordinarySha=sha(ordinary.bytes),freshSha=sha(fresh.bytes);if(ordinary.response.status===200&&fresh.response.status===200&&ordinarySha===wantedSha&&freshSha===wantedSha)return{rel,sha256:wantedSha,ordinary:ordinary.response.status,cacheBusted:fresh.response.status,cfCacheStatus:ordinary.response.headers.get('cf-cache-status'),age:ordinary.response.headers.get('age')};if(attempt===24)throw new Error(`Pages byte drift ${rel}: ordinary=${ordinary.response.status}/${ordinarySha} fresh=${fresh.response.status}/${freshSha} expected=${wantedSha} base=${base}`);await new Promise(r=>setTimeout(r,3000))}};
const results=[];let cursor=0;const worker=async()=>{while(cursor<files.length){const i=cursor++;results[i]=await verifyOne(files[i])}};await Promise.all(Array.from({length:Math.min(8,files.length)},worker));console.log(JSON.stringify({valid:true,base,verifiedFiles:results.length,stableMediaAliases:stable.aliases.length,ordinaryAndCacheBustedByteExact:true,results},null,2));
