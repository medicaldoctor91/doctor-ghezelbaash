import path from 'node:path';
import {readFile,readdir,access} from 'node:fs/promises';
import {createHash} from 'node:crypto';
import {assembleCanonicalContent} from './lib/assemble-content.mjs';
import {analyzeGraphClosure} from './lib/graph-integrity.mjs';
const root=process.cwd(),data=path.join(root,'src/data');const readJson=async p=>JSON.parse(await readFile(path.join(root,p),'utf8')),fail=m=>{throw new Error(m)},arr=v=>Array.isArray(v)?v:(v==null?[]:[v]),id=v=>typeof v==='string'?v:v?.['@id'];
const release=await readJson('src/data/release.json'),releaseInvariants=await readJson('src/data/release-invariants.json'),graph=await readJson('src/data/semantic/knowledge-graph.jsonld'),visible=await readJson('src/data/visible-contract.json'),services=await readJson('src/data/service-registry.json'),answers=await readJson('src/data/answer-registry.json'),authority=await readJson('.release/policy/authority-surface-contract.json'),platform=await readJson('.release/policy/platform-contract.json'),headProfile=await readJson('src/data/semantic/head-profile.json'),hf=authority.surfaces.huggingFace;
for(const f of ['CITATION.cff','codemeta.json','src/content-source/page.md','src/layouts/BaseLayout.astro','src/lib/css-delivery.mjs','src/lib/hero-image-contract.mjs','src/lib/release-tokens.mjs','src/data/semantic/head-ids.json','src/data/semantic/head-profile.json','src/data/semantic/support-ids.json','src/data/semantic/shapes.ttl','src/data/evidence-registry.json','src/data/evidence-snapshot.json','src/data/volatile-facts.json','src/data/render-calibration.json','.release/policy/platform-contract.json','.release/policy/authority-surface-contract.json','scripts/lib/css-rules.mjs','scripts/lib/graph-integrity.mjs','scripts/lib/release-graph.mjs','scripts/generate-retrieval-projections.mjs','scripts/generate-descriptors.mjs','scripts/finalize-dist.mjs','scripts/promote-release.mjs','scripts/zenodo_release.py','scripts/validate-media-references.mjs','scripts/validate-release-contract.mjs','scripts/validate-platform-contract.mjs','scripts/verify-huggingface-authority.mjs'])await access(path.join(root,f));
const pkg=await readJson('package.json'),baseGenerator=await readFile(path.join(root,'scripts/generate-projections.mjs'),'utf8'),retrievalGenerator=await readFile(path.join(root,'scripts/generate-retrieval-projections.mjs'),'utf8'),descriptorGenerator=await readFile(path.join(root,'scripts/generate-descriptors.mjs'),'utf8'),finalizer=await readFile(path.join(root,'scripts/finalize-dist.mjs'),'utf8'),mediaReferenceValidator=await readFile(path.join(root,'scripts/validate-media-references.mjs'),'utf8'),documentHead=await readFile(path.join(root,'src/components/DocumentHead.astro'),'utf8'),baseLayout=await readFile(path.join(root,'src/layouts/BaseLayout.astro'),'utf8');
const scriptSteps=name=>String(pkg.scripts?.[name]||'').split('&&').map(x=>x.trim()).filter(Boolean),hasStep=(name,needle)=>scriptSteps(name).some(step=>step===needle||step.includes(needle));
if(pkg.scripts?.['validate:release-contract']!=='node scripts/validate-release-contract.mjs')fail('Generic release-contract validator wiring missing');
if(!hasStep('prepare:generated','validate:media-references')||!hasStep('prepare:generated','rdf:generate')||!hasStep('prepare:generated','npm run generate'))fail('Generated preparation architecture drift');
if(pkg.scripts?.['validate:media-references']!=='node scripts/validate-media-references.mjs')fail('Read-only media reference validator wiring missing');
if(/\bwriteFile\b|\bappendFile\b/.test(mediaReferenceValidator))fail('Media reference validation must never rewrite source');
const generateSteps=scriptSteps('generate');for(const expected of ['node scripts/generate-projections.mjs','node scripts/generate-retrieval-projections.mjs','node scripts/generate-descriptors.mjs'])if(!generateSteps.includes(expected))fail(`Canonical generator missing ${expected}`);if(generateSteps.indexOf('node scripts/generate-projections.mjs')>generateSteps.indexOf('node scripts/generate-retrieval-projections.mjs')||generateSteps.indexOf('node scripts/generate-retrieval-projections.mjs')>generateSteps.indexOf('node scripts/generate-descriptors.mjs'))fail('Canonical generator order drift');
if(pkg.scripts?.['descriptors:finalize']!=='node scripts/generate-descriptors.mjs --dist dist')fail('DIST descriptor finalization stage missing');
for(const required of ['astro build','npm run indexnow:prepare','npm run descriptors:finalize','node scripts/finalize-dist.mjs'])if(!hasStep('build',required))fail(`Build DAG missing ${required}`);
if(retrievalGenerator.includes('public/current-release-matrix.json'))fail('Current release matrix has multiple deployable writers');
if(/\bservice_id\s*:|\bservice_family\s*:/.test(retrievalGenerator))fail('Query Matrix legacy scalar service schema remains in generator');
if(!finalizer.includes("projections/current-release-matrix.json")||!finalizer.includes('writeFile(currentMatrixPath'))fail('Finalizer is not the sole current-release-matrix DIST composer');
if(/\bmutateRoute\b|\bsetDigest\b/.test(finalizer))fail('Finalizer still carries post-definition header policy patching');
if(documentHead.includes("replace(viewportTag")||documentHead.includes("replace(heroPreloadTag"))fail('DocumentHead still reorders the canonical Head template');
if(!baseLayout.includes("../lib/css-delivery.mjs")||!baseGenerator.includes("../src/lib/css-delivery.mjs"))fail('CSS delivery contract is not shared by Layout and generator');
for(const name of ['datapackage.json','croissant.json','dcat.ttl','void.ttl','linkset.json']){if(baseGenerator.includes(`writeFile(path.join(projections,'${name}')`))fail(`Base generator illegally writes descriptor ${name}`);if(finalizer.includes(`writeFile(path.join(dist,'${name}')`))fail(`Finalizer illegally rewrites descriptor ${name}`);if(!descriptorGenerator.includes(`writeFile(out('${name}')`))fail(`Descriptor generator missing canonical writer for ${name}`)}
if(authority.identitySource!=='src/data/release.json'||platform.canonicalUrl!==release.canonicalUrl||platform.repository!==release.dataset.github.repository.replace(/^https:\/\/github\.com\//,''))fail('Platform/authority policy source drift');
if(!release.medicalReviewedAt)fail('Explicit medicalReviewedAt missing');if(release.dataset.zenodo.conceptDoi===release.dataset.zenodo.versionDoi)fail('Concept DOI collapsed with current Version DOI');
const nodes=graph['@graph']||[],byId=new Map(nodes.filter(n=>n?.['@id']).map(n=>[n['@id'],n])),person=byId.get(release.primaryEntity.id),clinic=byId.get(release.clinic.id),dataset=byId.get(release.dataset.id);if(!person||!clinic||!dataset)fail('Person/Clinic/Dataset graph constitution broken');if(id(dataset.creator)!==release.primaryEntity.id||id(dataset.publisher)!==release.primaryEntity.id)fail('Dataset creator/publisher is not the physician');if(!arr(dataset.sameAs).map(id).includes(`https://www.wikidata.org/entity/${release.dataset.wikidata}`))fail('Dataset Wikidata identity missing');if(arr(dataset.sameAs).map(id).some(x=>/github\.com|huggingface\.co|doi\.org|zenodo\.org/.test(x||'')))fail('Source/distribution collapsed into Dataset sameAs');
const fullGraphOnlyMemberships=[
  ['https://www.ghezelbaash.ir/#organization-american-academy-of-anti-aging-medicine','https://www.wikidata.org/entity/Q4742869'],
  ['https://www.ghezelbaash.ir/#organization-international-association-for-physicians-in-aesthetic-medicine','https://www.wikidata.org/entity/Q15995193'],
];
const personMembershipIds=new Set(arr(person.memberOf).map(id));
for(const [organizationId,wikidataId] of fullGraphOnlyMemberships){const organization=byId.get(organizationId);if(!personMembershipIds.has(organizationId)||!organization||!arr(organization['@type']).includes('Organization')||!arr(organization.sameAs).map(id).includes(wikidataId))fail(`Full-graph membership drift ${organizationId}`)}
const graphClosure=analyzeGraphClosure(graph,{baseUrl:release.canonicalUrl});if(graphClosure.duplicateIds.length||graphClosure.danglingSameSiteCount>0)fail(`Graph closure drift: duplicates=${graphClosure.duplicateIds.length}, dangling=${graphClosure.danglingSameSiteCount}`);
const graphIds=new Set(byId.keys());for(const file of ['src/data/semantic/head-ids.json','src/data/semantic/support-ids.json'])for(const ref of await readJson(file))if(!graphIds.has(ref))fail(`${file} references missing graph node ${ref}`);
const offered=new Set([...arr(person.availableService).map(id),...arr(clinic.availableService).map(id)].filter(Boolean)),registered=new Set(services.services.filter(x=>x.publishable).map(x=>x.id));if(registered.size<100)fail('Service registry unexpectedly sparse');for(const x of registered)if(!offered.has(x))fail(`Registry service not projected: ${x}`);for(const x of offered)if(!registered.has(x))fail(`Projected service missing from registry: ${x}`);if(![...registered].some(x=>x.includes('botulinum-toxin-chronic-migraine')))fail('Migraine Botox offered-service identity missing');
for(const r of answers.answers){const q=byId.get(r.questionId),a=byId.get(r.answerId);if(!q||!a||id(q.acceptedAnswer)!==r.answerId)fail(`Answer Registry drift ${r.questionId}`)}
const assembled=await assembleCanonicalContent({root,graph});if(/{{[A-Z0-9_]+}}/.test(assembled.content)||!assembled.content.includes(`Version ${release.release}`)||!assembled.content.includes(`https://doi.org/${release.dataset.zenodo.versionDoi}`))fail('Canonical page assembly release binding drift');
const headIds=await readJson('src/data/semantic/head-ids.json'),personHeadProfile=headProfile.nodes?.[release.primaryEntity.id],allowedHeadMemberships=new Set(personHeadProfile?.refAllow?.memberOf||[]);if(headProfile.maxBytes!==releaseInvariants.maxHeadGraphBytes)fail('Head profile byte ceiling drift from release invariant');if(!Array.isArray(personHeadProfile?.include)||!personHeadProfile.include.includes('memberOf')||!Array.isArray(personHeadProfile?.refAllow?.memberOf))fail('Head Person membership projection policy incomplete');if(!baseGenerator.includes('headNodes.push(projectNode(node,headProfile.nodes?.[id]))')||!baseGenerator.includes('allow.includes(r)'))fail('Head generator no longer enforces the declarative Head profile');for(const [organizationId] of fullGraphOnlyMemberships)if(headIds.includes(organizationId)||allowedHeadMemberships.has(organizationId))fail(`Full-graph-only membership admitted by Head source policy ${organizationId}`);
const headNodes=headIds.map(ref=>byId.get(ref)).filter(Boolean),headRefs=new Set();const collectHeadRefs=value=>{if(Array.isArray(value))return value.forEach(collectHeadRefs);if(!value||typeof value!=='object')return;if(typeof value['@id']==='string')headRefs.add(value['@id']);for(const [key,item] of Object.entries(value))if(key!=='@id')collectHeadRefs(item)};headNodes.forEach(collectHeadRefs);const speakableId=`${release.canonicalUrl}#speakable-primary-content`,speakable=byId.get(speakableId);if(!headRefs.has(speakableId)||!headIds.includes(speakableId)||!speakable||!arr(speakable['@type']).includes('SpeakableSpecification'))fail('Speakable projection closure drift');const speakableSelectors=arr(speakable.cssSelector);if(!speakableSelectors.length||speakable.xpath)fail('Speakable selector contract drift');for(const selector of speakableSelectors){if(selector.startsWith('#')&&!assembled.content.includes(`id="${selector.slice(1)}"`))fail(`Speakable ID selector missing ${selector}`);if(selector.startsWith('.')&&!new RegExp(`class=["'][^"']*\\b${selector.slice(1)}\\b`).test(assembled.content))fail(`Speakable class selector missing ${selector}`)}
const contentFiles=(await readdir(path.join(root,'src/content-source'))).filter(x=>/\.(html|md)$/i.test(x)).sort();let source='';for(const f of contentFiles)source+=await readFile(path.join(root,'src/content-source',f),'utf8')+'\n';if(!source.includes('id="saeed-ghezelbash"'))fail('Protected H1 missing');for(const h of visible.protected.aggressiveHeadings)if(h.id&&!source.includes(`id="${h.id}"`))fail(`Required aggressive heading removed: ${h.id}`);for(const h of visible.protected.instagramHeadingLinks)if(h.id&&!source.includes(`id="${h.id}"`))fail(`Required Instagram heading link removed: ${h.id}`);if(!source.includes('google-maps-clinic-reputation-current'))fail('Existing visible reputation slot removed');
const robots=await readFile(path.join(root,'public/robots.txt'),'utf8');if(!robots.includes('Content-Signal: search=yes, ai-input=yes, ai-train=yes, use=full'))fail('Maximum Content-Signal policy drift');for(const bot of ['Google-Extended','GPTBot','OAI-SearchBot','ChatGPT-User','ClaudeBot','PerplexityBot','Applebot-Extended'])if(!robots.includes(`User-agent: ${bot}\nAllow: /`))fail(`AI/search crawler contract drift: ${bot}`);
const headers=await readFile(path.join(data,'templates/headers.template'),'utf8');if(!headers.includes('Content-Signal: search=yes, ai-input=yes, ai-train=yes, use=full'))fail('Headers Content-Signal contract drift');
for(const t of ['question-answering','text-retrieval','text-generation'])if(!hf.taskCategories.includes(t))fail(`HF task contract missing ${t}`);for(const l of ['fa','en','ar','ckb'])if(!hf.languages.includes(l))fail(`HF language contract missing ${l}`);
const redirects=await readFile(path.join(root,'public/_redirects'),'utf8'),redirectsSha=createHash('sha256').update(redirects).digest('hex'),sources=new Set();if(redirectsSha!==releaseInvariants.redirectsSha256)fail('Redirect source bytes differ from release invariant');for(const line of redirects.split(/\r?\n/).map(x=>x.trim()).filter(Boolean)){const [from,to,code]=line.split(/\s+/);if(!from||!to||code!=='301')fail(`Malformed redirect: ${line}`);if(sources.has(from))fail(`Duplicate redirect source: ${from}`);sources.add(from);if(to.includes('#')){const frag=decodeURIComponent(to.split('#')[1]||'');if(frag&&!source.includes(`id="${frag}"`)&&!source.includes(`id='${frag}'`))fail(`Redirect fragment target missing: ${line}`)}}
const calibration=await readJson('src/data/render-calibration.json'),widths=[360,390,430,768,1024,1440],base=calibration['360']?.chunks||[];if(!base.length)fail('Render calibration baseline empty');const ids=base.map(x=>x.id);for(const w of widths){const rows=calibration[String(w)]?.chunks||[];if(rows.length!==ids.length||rows.some((x,i)=>x.id!==ids[i]||!Number.isFinite(Number(x.h))||Number(x.h)<=0))fail(`Render calibration identity drift ${w}`)}
const css=await readFile(path.join(root,'src/styles/global.css'),'utf8'),calSha=createHash('sha256').update(await readFile(path.join(root,'src/data/render-calibration.json'))).digest('hex');if(!css.includes(`DIST_CHUNK_CALIBRATION_SHA256:${calSha}`))fail('CSS/render calibration hash drift');
console.log(JSON.stringify({stage:'SOURCE_SEMANTIC_CONTRACT',release:release.release,services:registered.size,answers:answers.answers.length,protectedAggressiveHeadings:visible.protected.aggressiveHeadings.length,protectedInstagramHeadings:visible.protected.instagramHeadingLinks.length,renderChunks:ids.length,canonicalWriterTopology:'PASS',platformContract:'PASS',authoritySurfaceContract:'PASS',headProjectionPolicy:'SOURCE_VALIDATED',generatedFilePrerequisite:'NONE',buildSourceMutation:'FORBIDDEN',descriptorWriterPhases:['source-input','dist-final'],integrity:'PASS'},null,2));
