import path from 'node:path';
import {createHash} from 'node:crypto';
import {readFile,writeFile} from 'node:fs/promises';
const root=process.cwd(),canonicalPath=path.join(root,'src/data/render-calibration.json'),inputPath=path.resolve(root,process.argv[2]||canonicalPath),cssPath=path.join(root,'src/styles/global.css');
const widths=[360,390,430,768,1024,1440],data=JSON.parse(await readFile(inputPath,'utf8')),fail=m=>{throw new Error(m)},number=v=>Number.isFinite(Number(v))?Number(v):fail(`Non-finite calibration value: ${v}`),format=v=>String(Number(number(v).toFixed(2)));
const baseline=data['360'];if(!baseline||!Array.isArray(baseline.chunks)||!baseline.chunks.length)fail('360px calibration baseline missing');const expectedChunks=baseline.chunks.length;
for(const width of widths){const entry=data[String(width)];if(!entry||!Array.isArray(entry.chunks)||entry.chunks.length!==expectedChunks)fail(`Calibration width/chunk drift ${width}: expected ${expectedChunks}`);if(!Number.isInteger(entry.total)||entry.total<100000)fail(`Calibration document height invalid ${width}`)}
const identity=baseline.chunks.map(({i,id,key})=>({i,id,key}));for(const width of widths)for(let i=0;i<expectedChunks;i++){const current=data[String(width)].chunks[i],expected=identity[i];if(current.i!==expected.i||current.id!==expected.id||current.key!==expected.key||number(current.h)<100)fail(`Calibration identity/height drift ${width}:${i}`)}
const rulesFor=(values,render)=>values.map((chunk,index)=>`#${chunk.id}{--cis:${render(chunk,index)}}`).join(''),media=[];media.push(`@media(max-width:360px){${rulesFor(data['360'].chunks,c=>`${format(c.h)}px`)}}`);for(let w=0;w<widths.length-1;w++){const fromWidth=widths[w],toWidth=widths[w+1],from=data[String(fromWidth)].chunks,to=data[String(toWidth)].chunks;const rules=rulesFor(from,(chunk,index)=>{const slope=(number(to[index].h)-number(chunk.h))/(toWidth-fromWidth),coefficient=slope*100,intercept=number(chunk.h)-slope*fromWidth;return `calc(${format(intercept)}px ${coefficient<0?'-':'+'} ${format(Math.abs(coefficient))}vw)`});media.push(`@media(min-width:${format(fromWidth+0.01)}px) and (max-width:${toWidth}px){${rules}}`)}media.push(`@media(min-width:1440.01px){${rulesFor(data['1440'].chunks,c=>`${format(c.h)}px`)}}`);
const canonicalBytes=Buffer.from(`${JSON.stringify(data,null,2)}\n`),sha=createHash('sha256').update(canonicalBytes).digest('hex'),block=`/*DIST_CHUNK_CALIBRATION_SHA256:${sha}*//*DIST_CHUNK_INTRINSIC_START*/${media.join('')}/*DIST_CHUNK_INTRINSIC_END*/`,css=await readFile(cssPath,'utf8'),pattern=/\/\*DIST_CHUNK_CALIBRATION_SHA256:[0-9a-f]{64}\*\/\/\*DIST_CHUNK_INTRINSIC_START\*\/[\s\S]*?\/\*DIST_CHUNK_INTRINSIC_END\*\//;if(!pattern.test(css))fail('Render calibration CSS boundary missing');const nextCss=css.replace(pattern,block),expectedRules=expectedChunks*7;if((nextCss.match(/#[A-Za-z][\w:-]*\{--cis:/g)||[]).length<expectedRules)fail('Generated render calibration rule count drift');await Promise.all([writeFile(canonicalPath,canonicalBytes),writeFile(cssPath,nextCss)]);console.log(JSON.stringify({applied:true,input:path.relative(root,inputPath),widths,chunks:expectedChunks,sha256:sha,cssRules:expectedRules},null,2));
